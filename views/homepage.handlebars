<div class="page-header">
  <div class="header-card">
    <h2>Tech Blog</h2>
  </div>
</div>

{{#each posts as |post| }}
<div class="home-post">
  <div class="home-post-header">
    <h2>{{post.title}}</h2><p>Posted by {{post.username}} on {{format_date post.date_created}}</p>
  </div>

  <p>{{post.post}}</p>
</div>
{{/each}}
